# Data Pipeline

This document describes the pipeline as it currently exists. It is descriptive, not yet a fully reproducible workflow.

## 1. Raw and Curated Data

Large data live outside git under the local thesis data folder:

```text
E:\OneDrive\Studia\Studia magisterskie\Masterarbeit 2 - Sozialwissenschaften\data
```

Satellite products are curated into:

```text
...\data\Sat_data_curated
```

Each curated raster folder generally contains one or more GeoTIFFs plus a `curation_manifest.json`.

Examples:

```text
Age/
Height/
Population/
Surface/
Surface_NRES/
Volume/
Volume_NRES/
NTL/
PM_2_5_average/
PM_2_5_delta/
BC_average/
BC_delta/
OC_average/
OC_delta/
ERA5-Land_HDD/
ERA5-Land_CDD/
ERA5-Land_extreme_heat/
ERA5-Land_extreme_cold/
```

## 2. Satellite Preprocessing

Relevant files:

```text
preprocessing/curate_raw_tiffs.py
preprocessing/curate_pm_2_5.py
preprocessing/curate_oc_bc.py
preprocessing/ERA5L_Portugal_Indices_2010_2012_nativegrid.ipynb
```

Their responsibilities:

- ensure raster CRS is Mollweide where needed
- derive PM2.5 average and heating-season deltas
- derive OC/BC average and heating-season deltas
- derive ERA5-Land temperature indices
- write curation manifests for traceability

## 3. Freguesia Geometry

Tracked geometry files:

```text
data/parishes.geojson
data/parishes_bounding_box.geojson
```

`parishes.geojson` is the administrative unit geometry used for aggregation. It is tracked via Git LFS according to `.gitattributes`.

## 4. Indicator Manifest

Satellite-derived indicators are defined in:

```text
index_construction/indices_manifest.json
```

The manifest defines:

- input raster folders
- variable bindings
- per-cell expressions and masks
- aggregation/reduction methods
- output column names

The Excel workbook `index_construction/indices_manifest.xlsx` is a human-readable export of the same definitions.

## 5. Indicator Construction

Main script:

```text
index_construction/build_indices.py
```

The script streams over raster windows instead of loading full-country rasters into memory. It builds aligned VRT mosaics, rasterizes freguesia labels window-by-window, and computes zone-level aggregations.

Current output path from the manifest:

```text
E:\OneDrive\Studia\Studia magisterskie\Masterarbeit 2 - Sozialwissenschaften\data\outputs\indices
```

Expected generated files:

```text
freguesia_indices_streaming.csv
freguesia_indices_streaming.parquet
```

Note: the script currently assumes some relative imports and is safest to run from `index_construction/`.

## 6. Mapping / Exploration

Relevant file:

```text
data_exploration/map_index.py
```

It joins generated indicator CSVs to CAOP shapefiles from the external `Map/` folder and creates choropleth PNGs. The script currently points at `freguesia_indices_streaming.csv`.

## 7. Modeling

Relevant file:

```text
model/model_training.ipynb
```

The notebook currently sketches:

- satellite + basic administrative predictor assembly
- target loading from Gouveia et al. EPVI results
- fixed spatial holdout for IDs starting with `192` or `196`
- comparison of `HistGradientBoostingRegressor`, random forest, and ElasticNet

This is the next area to clean up methodologically.

## Administrative Predictor Assembly

Administrative predictors were not generated by a single script in this repo. They were downloaded as separate datasets and manually composed in:

```text
E:\OneDrive\Studia\Studia magisterskie\Masterarbeit 2 - Sozialwissenschaften\data\Adm_data
```

The composed output is:

```text
...\data\Adm_data\csv\all_used_adm_indicators.csv
```

The repo snapshot `data/all_used_adm_indicators.csv` is used by the early modeling work.
